# Pinned to the versions I actually ran this on, so a fresh clone reproduces my numbers.
# If you want exact hashes, run `pip freeze > requirements.lock` on your own machine.

pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
statsmodels==0.14.6
scikit-learn==1.8.0
pyyaml==6.0.3
matplotlib==3.10.8
pyarrow==25.0.1
pytest==9.1.1

# Only needed if you want to pull the real Criteo file instead of the simulated fallback.
# Uncomment before running the real-data path in the loader.
# scikit-uplift==0.5.1
